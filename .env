# Default environment (local development). Loaded by Expo CLI when NODE_ENV
# is unset or "development". Values here MUST NOT be secrets — they are
# inlined into the client bundle at build time.
EXPO_PUBLIC_API_BASE_URL=http://192.168.18.31:8080
EXPO_PUBLIC_API_TIMEOUT_MS=15000
